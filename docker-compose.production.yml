version: '3'

volumes:
  pg_data:
  static:

services:
  db:
    image: postgres:13
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    image: esaviv/kittygram_backend
    env_file: .env
  frontend:
    env_file: .env
    image: esaviv/kittygram_frontend
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static
  gateway:
    image: esaviv/kittygram_gateway
    env_file: .env
    ports:
      - 9000:80
    volumes:
      - static:/static
